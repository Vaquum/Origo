## What was done
- Landed the remaining Slice-34 correctness residue from stale local work onto a fresh `main`-based rescue branch so the already-frozen runtime/governance contracts are actually true in code: split exchange runtime defaults into explicit live/backfill helpers, hard-disabled the surviving historical helper write entrypoints, replaced the last hard-coded monthly Binance timeout with the existing env-backed Binance source timeout.
- Added focused rescue coverage in [test_backfill_runtime_contract.py](../../../tests/contract/test_backfill_runtime_contract.py), [test_s34_exchange_backfill_runner_contract.py](../../../tests/contract/test_s34_exchange_backfill_runner_contract.py), [test_s34_daily_dataset_tranche_controller_contract.py](../../../tests/contract/test_s34_daily_dataset_tranche_controller_contract.py), [test_s34_exchange_sequence_controller_contract.py](../../../tests/contract/test_s34_exchange_sequence_controller_contract.py), [test_s34_fred_backfill_contract.py](../../../tests/contract/test_s34_fred_backfill_contract.py), [test_s34_etf_backfill_runner_contract.py](../../../tests/contract/test_s34_etf_backfill_runner_contract.py), and [test_s34_bitcoin_backfill_runner_contract.py](../../../tests/contract/test_s34_bitcoin_backfill_runner_contract.py) so helper-write disablement and live-vs-backfill runtime-tag drift fail loudly.
- Froze the master doctrine as whole-system Origo law in [master-doctrine.json](../../../contracts/governance/master-doctrine.json), with `mechanical_enforcement_over_documentation` as the prime doctrine and a closed foundation set of `store_correctness`, `current_truth_read_contract`, `single_write_entrypoint`, `operator_truth_unification`, `envelope_finalization`, `concurrency_control`, `adapter_boundary_enforcement`, `rebuild_proof`, and `critical_path_viability`.
- Added the universal fail-closed task gates [task-start-gate.json](../../../contracts/governance/task-start-gate.json) and [task-end-gate.json](../../../contracts/governance/task-end-gate.json) so every task must pass the same doctrine questions before work starts and before any `done` / `ready` / `mergeable` claim.
- Rewired [AGENTS.md](../../../AGENTS.md), [governance-authority.json](../../../contracts/governance/governance-authority.json), [contract-applicability.json](../../../contracts/governance/contract-applicability.json), [spec/1-top-level-plan.md](../../1-top-level-plan.md), and [docs/Developer/README.md](../../../docs/Developer/README.md) so the master doctrine and task gates are universal routed contracts instead of optional reference material.
- Added focused governance coverage in [test_master_doctrine_contract.py](../../../tests/contract/test_master_doctrine_contract.py) and [test_task_gates_contract.py](../../../tests/contract/test_task_gates_contract.py), and extended the existing governance-structure / contract-applicability / task-admission / task-type tests so the repo fails loudly if the doctrine or gates stop being universal.
- Froze governance scope as whole-system Origo law instead of Slice-34-only law. `AGENTS.md`, `contracts/governance/governance-authority.json`, `contracts/governance/dagster-authority.json`, `contracts/governance/task-admission.json`, and `contracts/governance/request-task-coverage.json` now all state that routing and fail-closed rules apply to the full Origo system and all future development.
- Promoted the remaining spec-only runtime/data contract law into atomic machine-routed governance contracts: `storage-sql-discipline.json`, `serving-error-safety.json`, `integrity-durability.json`, `rights-secrets.json`, `runtime-recovery.json`, `ingestion-guarantees.json`, `raw-fidelity.json`, `event-sourcing.json`, `historical-surface.json`, `source-onboarding.json`, and `source-migration.json`.
- Replaced the slice-shaped request-family catalog with a whole-system family catalog that covers live proof, throughput proof, governance/review control, authoritative operator/user surfaces, runtime env/toolchain work, correctness repairs, performance repairs, and new system capability work without introducing a catch-all family.
- Expanded shared-domain overlay routing in `AGENTS.md` and `contracts/governance/contract-applicability.json` so every touched system surface now has to load an explicit machine contract or the agent must stop and report back before any mutation work begins.
- Added the final missing fail-closed governance admission layer: [request-task-coverage.json](../../../contracts/governance/request-task-coverage.json) now enumerates the exhaustive request-family catalog for whole-system Origo engineering work, maps each covered request family to exactly one primary task type, and declares any extra domain contracts that must be loaded before work starts.
- Rewired [AGENTS.md](../../../AGENTS.md), [spec/1-top-level-plan.md](../../1-top-level-plan.md), [docs/Developer/README.md](../../../docs/Developer/README.md), [task-admission-contract.md](../../../docs/Developer/task-admission-contract.md), and [task-type-contract.md](../../../docs/Developer/task-type-contract.md) so task start now means: match routed request family -> assign primary task type -> load routed contracts -> only then work.
- Added focused request-family coverage tests in [test_request_task_coverage_contract.py](../../../tests/contract/test_request_task_coverage_contract.py) and extended the existing governance-structure / task-type / task-admission / contract-applicability tests so the repo fails loudly if request-family coverage, admission rules, or shared-contract applicability drift apart.
- Refactored the governance package into the strict `task_type -> routing -> contract` hierarchy the repo now requires. `AGENTS.md` is the only governance routing layer, while `contracts/governance/` now contains atomic machine contracts for governance authority, execution doctrine, static analysis, work-plan discipline, documentation, slice closeout, task typing, PR routing, Dagster authority, and ingest throughput.
- Split the old monolithic task-type routing contract into [task-types.json](../../../contracts/governance/task-types.json) and [task-decomposition.json](../../../contracts/governance/task-decomposition.json), and removed the legacy combined contract file so task classification and decomposition remain separate machine concerns.
- Demoted [spec/1-top-level-plan.md](../../1-top-level-plan.md) and the developer governance reference docs to descriptive/reference-only status for governance. They now point at machine contracts instead of restating the rules as a second authority surface.
- Added focused governance-structure coverage in [tests/contract/test_governance_structure_contract.py](../../../tests/contract/test_governance_structure_contract.py) so the repo fails loudly if routing drifts away from `AGENTS.md`, if a legacy monolithic task-type contract returns, or if spec resumes carrying detailed normative governance prose.
- Derived the canonical task-type taxonomy from actual Origo commit/PR history at the title/summary level and froze it as repo law. The taxonomy now requires exactly one primary task type per work unit: `proof`, `governance`, `operator_surface`, `runtime_env`, `correctness`, `performance`, or `capability`.
- Added machine-readable task-type contracts [task-types.json](../../../contracts/governance/task-types.json) and [task-decomposition.json](../../../contracts/governance/task-decomposition.json), a live developer reference [task-type-contract.md](../../../docs/Developer/task-type-contract.md), focused contract tests [test_task_type_contract.py](../../../tests/contract/test_task_type_contract.py) and [test_governance_structure_contract.py](../../../tests/contract/test_governance_structure_contract.py), and deterministic baseline fixtures for the new governance surfaces.
- Tightened routing language so unknown-root-cause work must start as `proof` only, implementation after diagnosis must continue as a new task under the correct type, and mixed-type implementation branches/PRs are explicitly invalid.
- Audited the entire unmerged governance/runtime tranche against the immediately previous state and fixed the one real weakening risk: scheduled Dagster exchange runs could have silently inherited the backfill `deferred` projection default through a shared helper. The runtime contract now splits live schedule defaults from backfill defaults explicitly.
- Closed the remaining governance loopholes that still left room for “historical runner/controller” language to be misread as live permission. The runtime contract, work plan, and historical Slice-34 developer docs now preserve provenance while explicitly denying authority for helper-created write paths.
- Froze the live ingest-throughput contract as a machine-checkable law for first-party daily-file ingestion, with Binance daily as the default staged/vectorized pattern and no implicit live exceptions for OKX/Bybit.
- Froze the Dagster dashboard authority contract as whole-system Origo law by updating [AGENTS.md](../../../AGENTS.md), [spec/1-top-level-plan.md](../../1-top-level-plan.md), and [spec/2-itemized-work-plan.md](../../2-itemized-work-plan.md) so Dagster/Dagit is the sole authoritative partition truth, the sole allowed write entrypoint, and split-brain between Dagster and proof state is a hard failure.
- Added the machine-checkable governance contract [contracts/governance/dagster-authority.json](../../../contracts/governance/dagster-authority.json), the live developer reference [docs/Developer/dagster-authority-contract.md](../../../docs/Developer/dagster-authority-contract.md), and focused contract coverage in [tests/contract/test_dagster_authority_contract.py](../../../tests/contract/test_dagster_authority_contract.py), and removed the old runner docs from the live-contract index so they cannot remain canonical by accident.
- Froze the PR review-routing governance contract as whole-system Origo law by updating [AGENTS.md](../../../AGENTS.md), [spec/1-top-level-plan.md](../../1-top-level-plan.md), and [spec/2-itemized-work-plan.md](../../2-itemized-work-plan.md) so `zero-bang` is reviewer/approver only, never the PR author, and every merge path must flow through resolved conversations plus final re-requested review.
- Added the machine-checkable governance contract [contracts/governance/pr-review-routing.json](../../../contracts/governance/pr-review-routing.json), the live developer reference [docs/Developer/pr-review-routing-contract.md](../../../docs/Developer/pr-review-routing-contract.md), and focused contract coverage in [tests/contract/test_pr_review_routing_contract.py](../../../tests/contract/test_pr_review_routing_contract.py) so the routing rule cannot drift back into tribal knowledge.
- Added the `playwright` Python runtime dependency to the control-plane package and refreshed `control-plane/uv.lock` at version `1.2.69`.
- Patched [docker/Dockerfile.control-plane](../../../docker/Dockerfile.control-plane) to install the Debian/Chromium runtime libraries required by Playwright on the deployed control-plane image and to run `python -m playwright install chromium` during image build.
- Added [tests/contract/test_s34_etf_browser_runtime_contract.py](../../../tests/contract/test_s34_etf_browser_runtime_contract.py) so Slice 34 fails loudly if the control-plane dependency set, Docker runtime, or deploy workflow drops browser support again.
- Updated the Slice 34 work plan, top-level plan, changelogs, developer docs, and investigation log so the ETF browser runtime is an explicit hardened-runtime contract instead of tribal knowledge.
- Rewired [control-plane/origo_control_plane/jobs/etf_daily_ingest.py](../../../control-plane/origo_control_plane/jobs/etf_daily_ingest.py) so historical ETF backfill replays only archived issuer-source artifacts from object storage instead of issuing fresh live issuer requests during the Slice-34 backfill path.
- Added focused archive-replay contract coverage in [tests/contract/test_etf_daily_backfill_job_contract.py](../../../tests/contract/test_etf_daily_backfill_job_contract.py) for missing coverage, exact duplicate artifact deduplication, and fail-loud invalid archive behavior.
- Added deterministic latest-valid archive revision selection for ETF replay so the job chooses the newest valid archived artifact per issuer/day instead of failing on superseded revisions or irrelevant future captures.
- Extended [tests/contract/test_etf_daily_backfill_job_contract.py](../../../tests/contract/test_etf_daily_backfill_job_contract.py) with revision-precedence and ignored-invalid-artifact coverage.
- Added explicit ETF issuer-specific historical availability contracts in [control-plane/origo_control_plane/jobs/etf_daily_ingest.py](../../../control-plane/origo_control_plane/jobs/etf_daily_ingest.py) so required replay coverage is derived from source truth instead of from stale ETF canonical leftovers.
- Added [control-plane/origo_control_plane/s34_etf_ishares_archive_bootstrap_runner.py](../../../control-plane/origo_control_plane/s34_etf_ishares_archive_bootstrap_runner.py) to bootstrap official iShares `asOfDate` artifacts into raw-artifact storage with validation before persistence.
- Added focused contract coverage in [tests/contract/test_s34_etf_ishares_archive_bootstrap_runner_contract.py](../../../tests/contract/test_s34_etf_ishares_archive_bootstrap_runner_contract.py) and extended [tests/contract/test_etf_daily_backfill_job_contract.py](../../../tests/contract/test_etf_daily_backfill_job_contract.py) so Slice 34 fails loudly if issuer availability boundaries drift again.
- Updated Slice 34 docs, changelogs, and closeout artifacts to record the honest ETF historical contract: iShares is the only current first-party historical issuer source, while the remaining ETF issuers stay capture-forward from their first valid archived artifact day.
- Added persisted iShares no-data evidence handling so official holiday / market-closure responses are archived and later honored as negative evidence instead of being misclassified as missing historical partitions.
- Extended ETF replay contract coverage so iShares archived no-data artifacts remove official holiday dates from required coverage without weakening fail-loud behavior for genuine missing historical days.
- Added explicit zero-history handling for snapshot-only ETF issuers so `archive_capture_forward` sources with zero valid archived artifacts do not block replay for issuers with non-empty historical claims, while still surfacing those zero-history sources in proof output.
- Added [origo/events/storage.py](../../../origo/events/storage.py) plus ClickHouse migrations `0045__create_canonical_partition_reset_boundaries.sql` and `0046__create_canonical_event_log_active_v1.sql` so Slice 34 has an audited logical reset boundary contract over the append-only canonical event log.
- Rewired live canonical runtime reads in [origo/events/backfill_state.py](../../../origo/events/backfill_state.py), [origo/events/projector.py](../../../origo/events/projector.py), [origo/events/writer.py](../../../origo/events/writer.py), [control-plane/origo_control_plane/s34_partition_authority.py](../../../control-plane/origo_control_plane/s34_partition_authority.py), and the exchange fast-insert utilities so live proof/planner/projector/writer-identity truth now comes from `canonical_event_log_active_v1` instead of the base append-only table.
- Patched [control-plane/origo_control_plane/jobs/etf_daily_ingest.py](../../../control-plane/origo_control_plane/jobs/etf_daily_ingest.py) and [control-plane/origo_control_plane/jobs/fred_daily_ingest.py](../../../control-plane/origo_control_plane/jobs/fred_daily_ingest.py) so explicit reconcile reset-and-rewrite records a partition reset boundary and clears only projection/checkpoint/watermark state instead of issuing destructive `canonical_event_log` deletes.
- Added focused reset-boundary contract coverage in [tests/contract/test_canonical_event_reset_boundary_contract.py](../../../tests/contract/test_canonical_event_reset_boundary_contract.py) and extended the ETF/FRED/writer/planner contracts so the append-only reset contract fails loudly on drift.
- Replaced the hidden FRED revision-history initial vintage-window constant with required env `ORIGO_FRED_REVISION_HISTORY_INITIAL_VINTAGE_DATES_PER_REQUEST`, wired it through [origo/fred/client.py](../../../origo/fred/client.py), and tightened [tests/contract/test_fred_client_contract.py](../../../tests/contract/test_fred_client_contract.py) so Slice 34 fails loudly on missing/invalid transport-window config.
- Patched [deploy-on-merge.yml](../../../.github/workflows/deploy-on-merge.yml) so `ORIGO_FRED_REVISION_HISTORY_INITIAL_VINTAGE_DATES_PER_REQUEST` is now read from root `.env.example`, validated, transferred to the remote deploy script, required/range-checked on the host, rewritten into `/opt/origo/deploy/.env`, and surfaced in deploy logs instead of being silently omitted from the live runtime.
- Added [test_s34_fred_deploy_env_contract.py](../../../tests/contract/test_s34_fred_deploy_env_contract.py) so Slice 34 fails loudly if deploy ever stops synchronizing the required FRED revision-history window env again.
- Added [s34_fred_reconcile_planning.py](../../../control-plane/origo_control_plane/s34_fred_reconcile_planning.py) as the shared env-backed FRED reconcile planner contract for ambiguous-prefix and source-window bounded selection.
- Rewired [s34_fred_backfill_runner.py](../../../control-plane/origo_control_plane/s34_fred_backfill_runner.py) and [fred_daily_ingest.py](../../../control-plane/origo_control_plane/jobs/fred_daily_ingest.py) so direct Dagster/manual `reconcile` launches without explicit partition ids use the same bounded planner contract as the repo-native runner instead of silently re-expanding to the full FRED ambiguity set.
- Extended [test_s34_fred_backfill_contract.py](../../../tests/contract/test_s34_fred_backfill_contract.py) so planner drift between the FRED runner and Dagster job fails loudly.

## Current state
- Scheduled exchange Dagster runs now use explicit live runtime tags, while dashboard/backfill launches use explicit backfill runtime tags. The runtime no longer relies on one shared helper whose default could silently drift one path onto the other.
- The surviving historical Slice-34 helper runners/controllers are now hard-disabled for write execution in code, not just in docs. They can preserve provenance, but they cannot be used to mutate canonical state from outside Dagster.
- Monthly Binance now reads its source HTTP timeout from the same env-backed Binance timeout contract as the other exchange paths; there is no remaining hard-coded `60` second timeout in that legacy asset path.
- Every Origo task now has one topmost whole-system doctrine and two universal fail-closed gates. If a routed task cannot satisfy the master doctrine, the start gate, or the end gate, the agent must stop and report back before work or before any completion claim.
- The foundation set is now explicit and closed in-repo. No future work may claim “foundation” status by interpretation; anything outside the closed set is deferrable until forced unless the operator explicitly expands the set first.
- Governance is now whole-system law in-repo, not just active-slice law. Open slice boundaries no longer narrow the authority of `AGENTS.md` or any machine contract under `contracts/governance/`.
- The machine governance layer now covers both the routing system and the remaining core Origo runtime/data law that used to live only in spec prose. Future work touching storage, serving, durability, rights, recovery, ingestion guarantees, fidelity, event sourcing, historical surfaces, onboarding, or migration now has an explicit routed machine contract.
- Request-family coverage is now broad enough for future Origo engineering work while still remaining fail-closed: if a request touches a new recurring work class or a system surface with no routed domain overlay, the agent must stop and escalate before implementation.
- The governance system is now fail-closed at task start. No work may proceed until the request matches one routed request family, inherits or assigns exactly one primary task type, and loads every routed machine contract that applies to that family and task type.
- The request-family catalog is explicit about the recurring work classes that have already occurred in Origo history: live Dagster proof/audit work, ingest hot-path proof, governance/routing changes, review-routing changes, Dagit operator-surface changes, env/deploy wiring, correctness repairs, throughput tuning, and new dataset/job/daily-file capability work.
- Standalone reference-sync, closeout-sync, and review-follow-up work are no longer loopholes. They now require an explicit governing primary scope or the agent must stop and report back before touching the repo or runtime.
- The governance hierarchy is now explicit and single-cut: `AGENTS.md` routes work, `contracts/governance/*.json` governs it, and spec/developer docs only describe or reference it.
- There is no longer a monolithic task-type routing contract. Task taxonomy and mixed-type decomposition are now separate atomic machine contracts.
- The top-level plan no longer carries parallel rule prose for PR routing, Dagster authority, ingest throughput, task typing, static analysis, documentation, or closeout. Those rules now live in dedicated machine contracts and the spec only points to them.
- The repo now has a canonical task-type routing layer grounded in observed Origo work history. The agent no longer has license to improvise task framing: every requested work unit must have one primary task type, and multi-type work must be decomposed before coding.
- `proof` is now explicitly a non-implementation task type. It can gather evidence and isolate root cause, but it cannot silently drift into runtime mutation.
- The current unmerged governance/throughput tranche only strengthens the previous state. The discovered regression risk was removed by splitting `default_exchange_live_runtime_tags()` from `default_exchange_backfill_runtime_tags()`, so scheduled exchange runs remain explicitly `inline` while dashboard/backfill defaults remain explicitly `deferred`.
- Historical Slice-34 helper surfaces are now archival only in both prose and runtime behavior: the docs call them historical provenance, and the code-path entrypoints fail closed for write execution.
- The live ingest-throughput contract now names the covered daily-file channels (`binance_spot_trades`, `okx_spot_trades`, `bybit_spot_trades`) and the current live exception list is empty.
- Dagster/Dagit dashboard authority is now an explicit repo contract instead of an assumption: it is the sole operator truth for partition state, the sole allowed write entrypoint for backfill/reconcile, and any Dagster/proof mismatch is defined as a hard failure.
- The live Slice 34 runtime doc now treats dashboard launch as the canonical operator path and no longer advertises repo-native runner execution as a valid write path.
- The developer-doc live contract index now points at the Dagster authority contract and no longer promotes the Slice-34 runner/controller docs as canonical runtime truth.
- PR routing is now an explicit repo contract instead of operator memory: `zero-bang` may review and approve but may not author normal engineering PRs, and merge readiness now requires the full request-review -> resolve-conversations -> re-request-review -> approve sequence.
- Local proof shows the control-plane image now builds successfully with Playwright and Chromium installed.
- Deterministic local replay passed twice with the same validation summaries and the same Docker image id `sha256:d5bcb93c91764e241f7e6d003d733cd561872e4915800086033c77582a1b45a8`.
- `S34-C5f` is live-proven on `main`: the deployed ETF Dagster path now gets past the missing-Playwright failure and reaches real issuer payload parsing.
- `S34-C5e` is live-proven on `main`: the ETF backfill path is now archive-only and no longer falls back to live issuer requests.
- `S34-C5g` is implemented on the current branch but still needs merge, deploy, and live rerun proof. The next honest live ETF failure after this branch should be missing valid archive coverage, not duplicate-revision or future-capture noise.
- `S34-C5h` is live-proven on `main`: ETF historical replay now derives expected coverage from explicit issuer contracts instead of canonical ETF leftovers, and the repo has a dedicated iShares archive bootstrap runner for building missing historical raw-artifact coverage.
- `S34-C5i` is live-proven on `main`: iShares no-data holiday responses now persist as first-party negative evidence, and ETF replay excludes only those explicitly archived no-data dates from official iShares required coverage.
- `S34-C5j` is implemented on the current branch and locally proven. Snapshot-only issuers with zero valid archived artifacts now have an explicit empty historical claim, so they no longer masquerade as historical coverage gaps for the issuers that do have archive-backed history.
- Live evidence after merged `#104` showed the old FRED proof-mismatch blocker is genuinely gone: Dagster got into the audited reset path for `fred/fred_series_metrics/1947-02-01`, but the run then hung on a giant synchronous `canonical_event_log` delete mutation against one March-2026 part.
- `S34-C2l` is implemented and locally proven on the current branch: `canonical_event_log` remains the append-only write table, `canonical_event_log_active_v1` is the live read contract, and ETF/FRED reconcile resets now use audited logical reset boundaries instead of destructive canonical deletes.
- `S34-C6f` is now implemented and locally proven on the current branch: the live-safe FRED revision-history window is an explicit env-backed runtime contract instead of a hidden code constant, and the client still deterministically splits remaining `400`/`414`/timeout failures.
- Live Dagster rerun `5bc5d7ae-7538-4b52-ae16-51b2dc647b93` proved the remaining blocker after `S34-C6f` was deploy drift, not FRED client logic: the merged runtime contract existed in code and `.env.example`, but the deployed server env file still lacked `ORIGO_FRED_REVISION_HISTORY_INITIAL_VINTAGE_DATES_PER_REQUEST`.
- `S34-C6l` is now implemented and locally proven on the current branch: deploy-on-merge carries the required FRED revision-history window env through the same fail-loud sync path as the other runtime env keys, and focused workflow contract coverage pins that behavior.
- `S34-C6m` is now implemented and locally proven on the current branch: direct Dagster/manual FRED `reconcile` launches without explicit partition ids use the same env-backed bounded planner as the repo-native runner, so the job cannot silently widen back to the full ambiguity set.
- The next honest proof step is merge/deploy plus the same live FRED rerun. Success there means the reset phase completes without `canonical_event_log` mutation stall and the targeted partition reaches fresh terminal proof.
- The next honest proof step for `S34-C6m` is a direct Dagster/manual `reconcile` rerun after deploy, with Dagster logs proving that `partition_scope_count` now reflects the bounded subset rather than the pre-fix full ambiguity set.
- The current server archive remains sparse. Historical ETF completeness cannot be claimed beyond the archived issuer artifacts already stored under `raw-artifacts/`.

## Watch out
- The archived Slice-34 helper modules now carry explicit module-level `pyright` unused-function suppression because their write entrypoints are fail-closed while their historical planning helpers remain preserved for provenance and test coverage. Any future attempt to reactivate those surfaces should remove the suppression only after the module is either deleted or made live again under a real Dagster-native contract.
- The master doctrine is now the topmost governance law. If a future contract, helper, or runtime path conflicts with it, the doctrine wins and the lower surface must be changed or removed rather than explained away.
- The start/end gates only work if future governance additions keep routing through `AGENTS.md`. Adding a new machine contract without adding the route in `AGENTS.md` is now a direct contract violation.
- The shared-domain overlay list is now part of the governance law. If a future change adds a new durable system surface and does not add a matching routed machine contract, that omission is a hard stop, not something an agent may paper over with judgment.
- The new whole-system contracts are authoritative governance mirrors of the spec-era law, not permission to stop maintaining the live runtime docs. Runtime behavior still has to be updated and live-proved through the Dagster-authoritative path.
- If a future operator request falls outside the current request-family catalog, that is now an intentional hard stop, not a place for agent interpretation. The next governance change must add the new request family to the machine contract before implementation starts.
- Request-family coverage is supposed to be derived from actual repo work history, not invented on the fly. Any future family addition should be justified by a real recurring work class, not by one-off convenience wording.
- Any new governance aspect must be added in two places only: first to `AGENTS.md` routing, then to a new or updated atomic machine contract under `contracts/governance/`. Adding prose to spec/docs without that routing step is now a contract violation.
- Governance fixtures are now structure-sensitive. If a future change splits or merges a governance contract, every affected governance baseline fixture must be regenerated together; leaving one fixture on the old file topology will reintroduce a stale second truth.
- The new task-type contract is governance truth until it is live-proven in the real review/deployment flow. It prevents ambiguous task framing in-repo, but it still depends on disciplined enforcement when requests are decomposed into branches/PRs.
- Standalone type-checking of the archived helper modules still reports private-unused-function noise because those modules intentionally retain historical planning helpers while their write entrypoints are disabled. Behavioral enforcement is carried by the fail-closed runtime checks plus the helper-entrypoint contract tests, not by pretending those archived helpers are part of the live write path.
- The ingest-throughput contract is intentionally scoped to first-party daily-file ingestion. ETF, FRED, Bitcoin, and legacy monthly Binance are not implicit exceptions; each still needs its own explicit live throughput/runtime contract.
- The new Dagster authority contract is governance and documentation truth first. The runtime still needs explicit code-path cleanup and live proof so no historical helper path can write or paint Dagster red against green proof state.
- Historical Slice-34 runner/controller docs still exist in the repo as slice records. They are no longer live canonical contracts, but engineers can still misread them if they skip the live-contract index.
- The repo now encodes the PR-routing rule, but GitHub-side reviewer/branch-protection settings still need to continue matching the contract. If the hosted repo ever allows a merge path that bypasses final `zero-bang` approval, hosted policy has drifted away from repo truth.
- Slice 34 as a whole is still open; these artifacts only close the local proof legs for `S34-C2l`, `S34-C5g`, and `S34-C5j`.
- The live ETF rerun for `S34-C5g` must happen on the deployed server Dagster runtime because the real proof depends on the actual archive inventory and current archived revision mix.
- The live ETF proof for `S34-C5j` must verify that zero-history snapshot-only issuers are surfaced as such without blocking ETF replay progress for the issuers whose historical claim is non-empty.
- Any live runtime query that still reads `canonical_event_log` directly will bypass reset boundaries, re-surface stale pre-reset rows, or tempt operators back toward destructive reset mutations. The active-view read contract has to remain explicit and broad.
- Any future FRED planning change must keep the runner and the Dagster job on one shared planner implementation. If those paths drift again, dashboard/manual Dagster launches will silently bypass the safety ceilings even while the repo-native runner remains correct.
- The active view is a logical truth surface, not a storage compaction mechanism. Old canonical rows still exist physically for audit/provenance, so debugging and one-off SQL must choose intentionally between the append-only base table and the boundary-aware active view.
- The first live proof after deploy must inspect Dagster error logs and ClickHouse mutation state again. If a new run still opens a `canonical_event_log` delete mutation, the patch is incomplete.
- `ORIGO_FRED_REVISION_HISTORY_INITIAL_VINTAGE_DATES_PER_REQUEST` is now part of the live FRED transport contract. If server env sync misses it or drifts away from the proved timeout/window pair, the FRED client will now fail at startup/build time instead of silently using an old hard-coded value.
- The current public FRED Dagster GraphQL endpoint is reachable by raw host `159.69.57.19:14000`, while the older hostname route is not currently the reliable debugger path. Live reruns and failure inspection should keep using the endpoint that actually returns Dagster GraphQL data.
- Invalid archived issuer artifacts are still not acceptable historical coverage. The new selector only stops them from poisoning unrelated required windows; it does not make them valid.
- The currently known Grayscale artifact on the live server is a `429` security-checkpoint payload. If no valid archived Grayscale artifact exists for the required historical window, replay must still fail loudly on missing valid coverage.
- ETF historical completeness still depends on building out real archive coverage for every required issuer/day. This branch makes the runtime honest about that boundary, but it does not create history for the snapshot-only issuers.
